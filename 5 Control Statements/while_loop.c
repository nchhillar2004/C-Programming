#include <stdio.h>

int main(){
    int a = 10;

    // Loop runs while the condition is true
    while (a < 20){
        printf("Value of a is : %d\n", a);
        a++; //Increase value of a by +1 evertime the loop runs
    }

    return 0;
}